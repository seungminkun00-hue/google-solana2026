# 앱 화면 작업 현황 — 2026-08-02

Figma 목업 6화면을 실제 백엔드에 붙인 작업의 인수인계 문서.
다음에 이어서 할 때 이 문서부터 읽으면 된다.

---

## 심사위원 단독 시연 모드 (2026-08-02 저녁)

심사위원이 이 웹페이지 하나만 보고, 옆에서 설명해 줄 사람 없이 끝까지
가게 만드는 작업. 여섯 가지가 바뀌었다.

### 1. 데모봇을 미리 만들지 않는다

기동 시 `make_demo_bots()` 를 부르지 않는다(`app/main.py`). 앱을 열면 봇
목록이 비어 있고, 심사위원이 [+ 추가하기] 로 자기 봇을 만드는 데서 시작한다.
운영 상태(`state/app_state.devnet.json`)의 기존 봇 4마리는 포지션을 청산한 뒤
삭제했다.

정의는 남아 있다 — `POST /demo/seed` 가 여전히 bot1~3 을 만들고,
`verify_scenario.py` 와 `audit.py` 는 그 라우트를 쓰므로 검증 경로는 그대로다.
데모봇을 다시 띄우려면 `DEMO_BOTS=1` 로 기동한다.

### 2. Gemini 실추론 (BYOK)

`INFERENCE_MODE=byok` + `GEMINI_API_KEY` 로 구글에 직접 붙는다
(`app/adapters/gemini_byok.py`). 키는 `.env` 에 있고 `.gitignore` 에 걸어뒀다.

기존 `managed`(pay.sh mainnet 실결제) 경로는 그대로 살아 있다. 새 모드는
**결제 축을 건드리지 않는다** — API 호출 요금은 내장 x402 페이월을 통해
devnet USDC 로 그대로 흐르고, 바뀌는 것은 '누가 판단하느냐' 하나다.

**이 키로 실측한 것 (2026-08-02):**

| 모델 | 결과 |
|---|---|
| `gemini-3.1-pro-preview` · `gemini-3-pro-preview` | **429** — free tier `limit: 0`. 호출 자체가 안 된다 |
| `gemini-3.6-flash` | 1.6초 ↔ 45초 타임아웃 ↔ 429. 편차가 크다 |
| `gemini-3.1-flash-lite` | 1.6~1.8초로 일정 |

그래서 심층 추론도 Pro 가 아니라 Flash 계열이고, 기본값은 `flash-lite` 다.
화면의 모델 목록에서도 "Gemini 3.1 Pro" 를 뺐다 — 고르면 매번 폴백하는
선택지를 남겨두면 사용자는 Pro 로 판단한다고 믿게 된다.
**실제로 답한 모델 ID** 가 영수증·API 탭·대화 말풍선에 그대로 표시된다.

### 3. 룰북 → Gemini 지침

봇을 만들면 룰북과 프로필에서 시스템 프롬프트가 조립된다
(`app/core/prompts.py`). 생성 직후 시트로 원문을 보여주고, 설정 화면의
「AI에게 저장된 지침 보기」 에서 언제든 다시 볼 수 있다.

문자열을 저장하지 않고 매번 조립하는 이유: 저장해두면 나중에 룰북을 고쳤을 때
그 문자열만 옛날 값으로 남는다. 화면은 "저장된 지침"이라며 종목 3개를
보여주는데 실제 판단은 5개 기준으로 도는 상태가 된다.

### 4. 대화는 주식 이야기만

`POST /ui/bots/{id}/chat` 이 두 겹으로 막는다.
① 모델이 `{"on_topic": bool, "reply": str}` 로 답해 주제 이탈을 스스로 판정
② 모델을 못 부르면(키 없음·429) 서버가 단어 기준으로 직접 가른다

②가 없으면 규칙이 외부 서비스의 가용성에 달리게 된다. 숫자는 항상 원장에서
뽑아 프롬프트에 넣어주고, 없는 숫자는 모른다고 답하게 한다.

### 5. 체결 알림 · 온체인 증거

- 매수·매도가 잡히면 화면 위에서 알림이 내려온다(`web/src/components/FillToasts.tsx`).
  재료는 `GET /ui/events?since=` — 단조 증가 seq 라 같은 초에 두 건이 들어와도
  놓치지 않는다. 첫 요청은 위치만 받아와 지난 체결을 쏟아내지 않는다.
- 거래 내역의 각 행과 API 탭의 **최근 결제 내역** 에 devnet 서명이 붙는다.
  누르면 Explorer 로 간다. 호출 한 건 = 결제 한 건 = 서명 하나를 직접 대조할 수 있다.
- mock 원장에서는 링크를 만들지 않는다 — 없는 트랜잭션으로 보내면 더 나쁘다.

### 6. 옆의 진행 안내

목업 옆 패널이 「진행 안내」와 「심사위원 지갑 시연」 두 탭이 됐다
(`web/src/components/Sidebar.tsx`). 기본은 안내다.

7단계를 순서대로 짚어주는데, **순서를 세지 않고 각 단계의 '끝났다고 볼 조건'**
으로 현재 위치를 정한다(`GuidePanel.tsx`). 조건의 재료는 전부 실제 상태다 —
봇이 있는가(서버), 체결이 있는가(서버), 어느 화면을 지나왔는가(라우터).
그래서 심사위원이 순서를 건너뛰거나 되돌아가도 안내가 어긋나지 않는다.

봇 요약 탭 맨 위에 **[지금 일해보기]** 가 생겼다(`POST /ui/bots/{id}/run`).
누르면 뉴스 구매 → 스크리닝 → 심층 추론 → 룰북 → 체결이 실제로 일어나고
단계 로그가 그대로 나온다. 15~30초.

### 7. 종목이 아니라 시장을 고른다 (2026-08-02 밤)

봇 설정에서 개별 종목 선택을 없앴다. 사용자는 **어느 장에서 놀지**만 정하고,
그 안에서 무엇을 살지는 봇이 뉴스와 추론으로 고른다 — 종목까지 찍어주면
남는 건 타이밍뿐이라 자동매매라고 부르기 어렵다.

시장 카탈로그는 `app/core/markets.py` 이고, 고른 시장은 곧 룰북의 허용 종목
목록으로 펼쳐진다. **집행되는 규칙은 여전히 화이트리스트**라 audit·verify 가
검증하는 성질은 그대로다. 바뀐 건 그 목록을 사람이 적느냐 시장이 펼치느냐뿐.

| 시장 | 상장 | 상태 |
|---|---|---|
| 미국 · 나스닥 빅테크 | NVDA · MSFT · AAPL · TSLA | 거래 가능 |
| 미국 · 반도체 | NVDA | 거래 가능 |
| 대한민국 · 코스피 / 코스닥 | — | **거래 불가** (devnet 미러 토큰 없음) |

코스피·코스닥은 화면에서 회색으로 두고 이유를 그 자리에 적는다. 고를 수 있게
해놓고 아무것도 못 사면 화면이 거짓말을 하는 것이다. 서버도 `400` 으로 막는다.
미러 토큰을 더 발행하면 `markets.py` 의 `tickers` 만 채우면 된다.

### 8. 대화가 상태를 통째로 본다

예전에는 질문의 단어를 보고 필요한 조각만 골라 한 문장으로 만들어 모델에게
넘겼다. 그래서 모델이 할 수 있는 일은 그 문장을 바꿔 쓰는 것뿐이었고,
"왜 아직 안 샀어?" 처럼 여러 사실을 엮어야 하는 질문에 엉뚱한 조각을 받아
바보처럼 답했다.

이제 `_facts()` 가 상태를 한 번에 다 준다 — 자산·지갑별 잔고·룰북·오늘 거래
횟수·보유 포지션별 손익과 보유시간·최근 판단의 근거 원문·최근 체결 5건·
누적 API 결제·적중률. 무엇이 답에 필요한지는 모델이 고른다.

화면이 앞선 대화 8턴을 함께 보내므로 맥락도 이어진다. 프롬프트는 "수치는 여기
있는 것만" 을 유지하되 **해석과 의견은 하라고** 바꿨다. 실측 응답:

> "현재 500.30 USDC를 전액 현금으로 보유 중인 이유는 아직 제 룰북의 매수 조건인
> '확신도 0.7'을 충족할 만한 분명한 AI 인프라 촉매가 감지되지 않았기 때문입니다…"

### 9. 안내가 지갑 연결부터 시킨다

단계 순서를 바꿨다: 앱 켜기 → **지갑 연결** → 봇 만들기 → **위임 서명** →
일 시키기 → 거래내역 → API → 대화. 지갑을 뒤로 미루면 봇을 만들고 나서야
팬텀 설치와 devnet 전환을 하게 되어 흐름이 끊긴다. 지갑 단계에는 「지갑 시연
탭 열기」 버튼이 붙어 탭을 찾아 헤매지 않는다. 완료 판정은 서버의
`/judge/status`(등록 여부·위임 한도)로 한다 — 브라우저의 팬텀 상태만 보면
'연결했다고 생각했는데 등록은 안 된' 경우를 놓친다.

### 10. 대화가 실제 기사를 읽는다 (2026-08-02 밤)

"애플 요즘 어때?", "호재 있어?" 에 잔고를 읊던 문제. 이제 질문에서 종목을
알아내 **실제 기사**를 받아와 프롬프트에 넣는다(`app/adapters/news.py`,
Alpha Vantage NEWS_SENTIMENT). 실측 응답:

> "애플은 AI 붐을 타고 긍정적 평가를 받고 있지만 보안팀 업무 과부하라는
> 이슈도 공존합니다. 기관 매수세는 고무적이나 제 AAPL 포지션은 -2.50% 로
> 손절선 -5.0% 에 근접해 있어 신중히 판단하겠습니다."

- 종목 인식은 한국어 별칭까지 본다(`markets.ALIASES` — "애플"·"엔비디아").
- 룰북 밖 종목도 **대화는 된다.** 룰북은 매매를 막는 것이지 대화를 막는
  것이 아니다. 못 산다는 사실만 한 줄 덧붙이게 했다.
- 뉴스는 질문에 종목이 있거나 시황을 묻는 말투일 때만 부른다 —
  무료 한도가 하루 25회라 "내 잔고 얼마야"에까지 붙이면 금방 소진된다.
- 모델이 429 면 기본 모델로 한 번 더 시도한다. 한도가 모델별로 따로
  걸려 있어서, 사용자가 고른 모델만 막힌 경우가 실제로 있었다.

> ⚠️ Alpha Vantage 는 `apikey` 파라미터가 **있기만 하면** 응답하고 값을
> 검증하지 않는다(임의 문자열도 통과 — 2026-08-02 실측). 지금 `.env` 값은
> 자리표시자다. 제대로 하려면 무료 키를 발급받아 바꾼다.
> Gemini 의 `google_search` 그라운딩은 이 키에서 quota 0 이라 못 쓴다
> (같은 모델에 일반 요청 200, 검색 도구 붙이면 429).

### 11. 실행 로그가 항상 오른쪽에 있다

예전에는 로그가 「심사위원 지갑 시연」 탭 안에만 있었다. 안내 탭을 보고
있으면 봇이 무엇을 하는지 아무것도 안 보였다.

이제 배치가 **[폰] [안내 또는 지갑시연] [실행 로그]** 이고, 가운데만 탭으로
바뀐다. 앱의 [지금 일해보기] 와 지갑 시연의 [매수 실행] 이 **같은 로그로
흐르고**, 줄 왼쪽 색으로 어느 경로인지 구분된다
(`components/activityLog.ts` · `ActivityPanel.tsx`).

### 12. 두 실행 버튼의 차이

| | 앱 · 지금 일해보기 | 지갑 시연 · ④ 매수 실행 |
|---|---|---|
| 돌리는 것 | `POST /ui/bots/{id}/run` | `GET /judge/buy` (SSE) |
| 앞에 붙는 것 | 없음 | **심사위원 지갑에서 $5 위임 인출** |
| 노출 한도 초과 시 | 그대로 진행 | 기존 포지션 먼저 청산 |
| 재시도 | 최대 2회 | 최대 3회 |
| 결과 | 끝나고 한 번에 | 단계별 실시간 스트림 |

**봇 사이클 자체는 완전히 같다.** 차이는 자금 출처 하나뿐이다 — 앱 버튼은
봇 지갑에서, 지갑 시연은 심사위원 본인 지갑에서 먼저 끌어온다. 그 인출에
서명이 필요 없다는 것이 지갑 시연이 보여주려는 전부다. 두 화면 모두 이
문장을 버튼 옆에 적어뒀다.

### 13. 실시세 — 한국투자증권 KIS OpenAPI (2026-08-02 밤)

`app/adapters/kis_quotes.py`. `.env` 의 `KIS_APP_KEY` + `KIS_APP_SECRET` 으로
켜지고, `external.spot()` 이 1순위로 쓴다. 실측 확인:

```
삼성전자 005930   262,500 KRW      (국내 · FHKST01010100)
AAPL / NAS           308.91 USD    (해외 · HHDFS00000300)
MSFT / NAS           464.72 USD
```

미러 토큰 매수가 **실제 시세로 체결된다** — MSFTx 를 $464.72 에 샀다.

- 토큰 발급은 **1분에 1회**만 된다 → `wallets/kis_token.json` 에 캐시.
  그 파일을 지우면 1분을 기다려야 한다.
- 초당 호출 제한(`EGW00201`)이 있다 → 호출을 직렬화하고 최소 250ms 간격,
  걸리면 1초 쉬고 한 번 재시도.
- 미러 토큰 표기를 그대로 보내면 **빈 응답**이 온다(`MSFTx` 라는 심볼은
  거래소에 없다). 접미사를 떼고 보낸다.
- 국내 종목은 원화로 오므로 환율로 USD 환산한다 — 안 하면 262,500 이
  $262,500 이 된다.

**`PRICE_SOURCE` 로 고른다**: `kis`(기본) · `live`(pay.sh) · `mock`(내장 기준가).
⚠️ `verify_scenario.py` 는 맨 위에서 `PRICE_SOURCE=mock` 을 세운다. 손절·익절을
증명하려면 기준가를 직접 움직여야 하는데, 실시세를 쓰면 그 조작이 무시돼
"가격을 -15% 내렸는데 손절이 안 걸린다"가 된다(실제로 그렇게 깨졌다).

새 종목을 붙이려면 `markets.QUOTE_SPEC` 에 국내는 `code`(6자리),
해외는 `excd`(NAS/NYS/TSE…)만 채우면 된다.

### 14. 매도·정산 — 돈이 심사위원 지갑으로 돌아온다

매수만 보여주면 "돈이 나가는 것"까지만 증명된다. 자동매매의 값어치는
돌아오는 쪽에 있는데, 정산 결과가 봇 지갑 안에서만 움직여서 심사위원
입장에서는 여전히 숫자놀음이었다.

| 어디서 | 무엇을 |
|---|---|
| 앱 요약 탭 `[전량 매도·정산]` | `POST /ui/bots/{id}/sell?all=true` — 청산·정산·분배 |
| 앱 (룰북 모드) | `?all=false` — 익절·손절·보유시간 조건에 걸린 것만 |
| 지갑 시연 ⑤ `[전량 매도하고 회수]` | `GET /judge/sell` (SSE) — 위 + **지갑으로 반환** |

경로 화이트리스트에 `("user-treasury", "judge-wallet")` 을 더했다.
⚠️ `("user-treasury", "external")` 은 **여전히 금지**다 — 그게 audit.py 6번이
검증하는 '사용자 원금 유출 차단'이고 그대로 통과한다. 다른 점은 목적지가
임의의 외부가 아니라 **그 돈을 넣은 본인 지갑**이라는 것이다.

실측 (2026-08-02):
```
balance-before  judge_balance 0
close-start     positions 1 (MSFTx)
settled         진입 $464.72 → 청산 $464.72 · 실현 -233µ · 분배 [] (손실이라 없음)
payout-done     472,688,685µ · tx 3QNajxMS…
balance-after   judge_balance 472,688,685  (gained 472,688,685)
```

손익을 만들어내지 않는다. 청산가는 매수와 같은 시세원(KIS)에서 오고,
분배 비율(85/10/5)은 매수 시점 영수증에 박제된 값을 그대로 쓴다.

### 15. 시장 4개 · 미러 토큰 76종 (2026-08-03)

| 시장 | 종목 | 예 |
|---|---|---|
| 대한민국 · 코스피 | 19 | 삼성전자 $182.52 · SK하이닉스 $1,194.55 |
| 대한민국 · 코스닥 | 20 | 에코프로비엠 $71.96 · 알테오젠 $214.50 |
| 미국 · 나스닥 | 19 | AAPL $308.91 · MSFT $464.72 |
| 일본 · 도쿄 | 18 | 토요타 $19.37 · 소니 $23.92 |

**코스피 봇이 SK하이닉스를 $1,194.55 에 실제로 샀다** (1,718,000원 환산가).

#### 목록을 손으로 적지 않는다

종목·이름·거래소를 코드에 박으면 반드시 틀린다. 그래서 두 단계로 만든다.

```
py -3.13 check_markets.py    KIS 로 한 종목씩 실제 조회 → 값이 오는 것만 남긴다
py -3.13 mint_markets.py     그 결과대로 devnet 민트 발행 (이미 있으면 건너뜀)
```

`markets.py` 는 두 파일의 **교집합**만 시장으로 만든다 —
`market_candidates.json`(시세가 오는가) ∩ `devnet.json[mirror_mints]`(민트가 있는가).
시세가 있어야 값을 매기고 민트가 있어야 산다. 하나라도 없으면 거래 불가로
표시하고 이유를 화면에 그대로 띄운다.

종목명도 KIS 에서 받는다(`search-stock-info`, CTPF1002R) — 시세 응답에는
이름이 없어서 따로 물어봐야 한다. 그 이름이 대화의 종목 인식 별칭도 된다
("삼성전자 어때?" 를 알아듣는다).

후보 80종 중 4종이 탈락했다(조회 실패). 남길 이유가 없어 뺐다.

#### 이 과정에서 고친 것

| 증상 | 원인 · 해결 |
|---|---|
| 봇 하나 만드는 데 계정 80개 | `ensure_bot_wallets` 가 전 종목 미러 ATA 를 미리 팠다. 4종일 땐 괜찮았지만 80종이면 임대료만 0.16 SOL. **`swap_in` 이 매수 시점에 그 종목만 멱등 생성**하도록 옮김 |
| 토요타가 $3,067 | 해외 시세 응답에 **통화 필드가 없다**(rsym·base·last·pvol·tvol 뿐). USD 로 가정해 엔화를 그대로 썼다 → 약 150배. 거래소→통화 표(`EXCHANGE_CURRENCY`)를 만들고 `fx.rate_to_usd()` 로 환산. 모르는 거래소는 추측하지 않고 거부한다 |
| 검증 스크립트가 17분간 멈춤 | `_spaced` 안에서 `_spaced` 를 다시 불러 같은 `asyncio.Lock` 을 두 번 잡았다(재진입 불가 → 영구 대기). 중첩 호출 금지를 주석으로 못박고 풀었다 |
| 코스피 봇이 아무것도 못 삼 | 뉴스 풀이 미국 4종뿐이라 국내 종목 뉴스가 아예 없었다. 시장 카탈로그에서 생성하도록 바꾸고, **정찰이 자기 룰북 범위로 검색을 좁히게** 했다 — 76종 중 자기 종목이 걸릴 확률이 너무 낮았다(돈만 쓰고 기회는 없음) |
| 국내·일본 종목을 룰북에 못 넣음 | `universe.is_tracked` 가 SEC 목록만 봤다. 시장 카탈로그도 함께 보게 함 — 그 종목들은 시세와 민트가 둘 다 확인된, 살 수 있음이 증명된 종목이다 |

### 16. 대화로 직접 주문 — 룰북보다 먼저 (2026-08-03)

"삼성전자 30달러어치 사줘" 라고 하면 **룰북 게이트를 거치지 않고 바로 산다.**
실측: 봇이 "확신도 기준을 못 넘어 매수하지 않겠습니다" 라고 판단한 상황에서도
지시대로 0.164365주를 $182.52 에 체결했다.

#### 왜 이것이 프로젝트의 주장을 훼손하지 않나

룰북은 **에이전트의 자율 판단**에 걸리는 규칙이다. "확신도 0.8 미만이면 사지
마라" 는 봇이 스스로 결정할 때의 기준이지, 소유자의 직접 주문까지 막으라는
뜻이 아니다. 증권사 앱에서 자동매매 조건을 걸어둬도 본인이 시장가 주문을 내는
것은 언제나 되는 것과 같다.

대신 **반드시 구분해서 기록한다** — `receipt.manual = True`, 체결 사유는
`manual`. 구분이 없으면 나중에 이 체결을 보고 "봇이 룰북을 어겼다"고 읽히고,
그건 사실이 아니다. 적중률(`RECEIPTS.stats`)에서도 빠진다 — 그건 에이전트의
성적표이고 만다트가 그걸 보고 추가 투입을 심사하기 때문이다.

#### 열쇠 두 개

모델이 주문 의사를 냈다는 것만으로는 집행하지 않는다. **사용자의 원문에도
지시하는 말이 있어야 한다.**

| 말 | 결과 |
|---|---|
| "삼성전자 살까? 어때?" | 의견만. 체결 없음 |
| "삼성전자 30달러어치 사줘" | **체결** |
| "엔비디아 다 팔아" | 그 종목 전량 청산·정산 |

지시 해석은 **서버가** 한다(`_parse_order`). 처음에는 모델의 `action` 만
집행했는데, 모델이 "제 운용 원칙에 맞지 않습니다" 라며 주문을 거절했다(실측).
주문은 모델이 판단할 일이 아니다. 모델의 응답은 금액·종목을 보태는 보조로만 쓴다.

집행되면 결과를 **먼저** 보여준다. 봇의 의견이 앞에 오면
"매수는 지양하겠습니다 … ✅ 매수했습니다" 로 앞뒤가 어긋나 읽힌다.

그래도 지키는 것: 돈이 있어야 사고, 미러 토큰이 있는 종목만 사고,
자금 경로 규칙(`core/routes.py`)은 그대로 통과한다.

### 17. 지갑이 왜 둘인가 — 그리고 앱도 심사위원 지갑에서 뺀다

**질문**: 데모 앱에서도 봇 지갑 말고 심사위원 지갑에서 바로 빼면 안 되나?

**답**: 지갑 자체를 하나로 합칠 수는 없다. 심사위원 지갑은 **우리가 개인키를
갖지 않는** 외부 지갑이라, 거기서 돈을 빼려면 본인 서명이 있거나 미리
위임(SPL approve)을 받아야 한다. 봇 지갑은 우리가 키를 들고 있어서 API 결제·
매수·정산을 사람에게 묻지 않고 집행할 수 있다 — 그게 '에이전트가 스스로
집행한다'는 주장의 물리적 근거다.

**다만 나눠 보일 이유는 없었다.** 이제 앱의 [매수 사이클] 도 위임이 있으면
심사위원 지갑에서 먼저 인출한다(`_pull_from_judge`). 위임이 없으면 봇 지갑
안에서 돈다 — 지갑을 안 붙인 사람도 앱만으로 전 과정을 볼 수 있어야 한다.

즉 두 버튼의 차이가 사라졌다. 남은 차이는 지갑 시연 쪽이 단계를 실시간
스트림으로 흘려보낸다는 것뿐이다.

### 18. 국기와 매수·매도 색

코인으로 사면 환전 없이 어느 나라 주식이든 같은 지갑에서 산다 — 그게 이
구조의 값어치인데 종목 코드만 보면 안 읽힌다. 그래서 🇰🇷 🇺🇸 🇯🇵 를
보유 종목·거래 내역·체결 알림·시장 선택·AI 요약 리포트에 붙였다.

AI 요약 리포트의 **매수는 빨강, 매도는 파랑**(한국 시장 관행). 글자만 칠한다.
손익 색(`--up`/`--down`)과는 다른 축이라 `--buy`/`--sell` 토큰을 쓴다 —
그 둘을 섞으면 "매도인데 초록" 같은 조합이 나온다.

### 19. 앱 안의 충전하기 · 새 도입부 이미지 · 로고 (2026-08-03)

**충전하기** 버튼이 실제로 동작한다. 봇과 금액을 고르면 연결된 지갑에서
그 봇의 지갑으로 devnet USDC 가 옮겨지고(`POST /judge/deposit`),
매수와 **같은 자리에 팝업**이 뜬다(`events.emit_deposit`).
위임 한도 안에서는 추가 서명이 없다 — 그게 이 제품의 주장이고, 충전 버튼이
그걸 가장 일상적인 모습으로 보여준다.

지갑이 없거나 위임이 없으면 버튼을 죽이지 않고 **무엇을 먼저 해야 하는지**
시트 안에 적는다. 비활성으로만 두면 왜 안 되는지 알 수 없다.

**도입부 이미지 교체.** 새 파일은 1206×2616 = 디자인(402×872)의 정확히 3배라
목업 안에서 선명하다. 아이콘 위치가 바뀌었으므로 클릭 영역을 **픽셀에서
다시 쟀다** — 눈으로 어림하면 아이콘을 눌러도 안 열린다.

```
파란 아이콘  원본 x 90..310 · y 267..487 (221px 정사각)  → 디자인 left 30, top 89, size 74
라벨 아랫변  원본 y 545                                  → 디자인 182
```

이미지를 또 바꾸면 이 값을 다시 재야 한다. 측정은 `IntroBoot.tsx` 주석에
방법까지 적어뒀다.

⚠️ springboard 는 **3.6MB** 다. 로컬 시연은 문제없지만 심사용 링크로 배포하면
첫 화면이 늦게 뜬다. 배포 전에 2배(804×1744)로 줄이거나 WebP 로 바꾸면
1/10 이하가 된다 — 402px 프레임에서는 2배로도 선명하다.

**파비콘** `public/favicon.png`(157×157). 옛 SVG 는 예비로 남겼다.

**신한·토스 로고.** 받은 이미지에 원형 테두리가 이미 들어 있어서
`ProviderMark` 가 배경 원을 그리지 않고 이미지를 그대로 놓는다 — 그리면
테두리가 두 겹이 된다. 두 곳 모두 추천 API 목록에 `ready: false` 로 올렸다.
어댑터도 계약도 아직 없다는 뜻이고, 그 사실을 화면이 그대로 말한다.

⚠️ **미검증**: 충전의 전 과정 중 **위임 서명**은 팬텀이 필요해 헤드리스로
확인하지 못했다. 라우트의 방어(위임 없음·한도 초과·잔고 부족)와 UI 는
확인했고, `delegated_transfer` 자체는 지갑 시연 ④ 에서 이미 검증된 경로다.

### 20. 세션 격리 — 여러 명이 같은 링크를 봐도 안 섞인다 (2026-08-03)

배포 전에 세 가지가 걸렸다.

| 문제 | 배포하면 |
|---|---|
| `BOTS` 가 서버 전역 딕셔너리 하나 | 내 테스트 봇이 심사위원에게 보이고, 심사위원 봇이 나에게 보인다 |
| `JUDGE_WALLET` 이 상수 하나(`judge-wallet@demo`) | A 가 팬텀을 연결한 뒤 B 가 연결하면 **B 가 A 를 덮어쓴다** |
| 관리자 토큰이 브라우저 번들에 있음 | 개발자도구를 열면 남의 봇을 지울 수 있다 |

**브라우저마다 세션 ID 를 발급해 봇과 심사위원 지갑을 거기 묶었다**
(`app/core/session.py`). 로그인이 아니다 — 신원 확인이 아니라 '같은
브라우저인가'만 가른다. 심사 링크에 회원가입을 붙일 수는 없다.

```
프론트  localStorage 에 24자 ID → 모든 요청에 X-Session 헤더
        (SSE 는 헤더를 못 붙여 쿼리로 넘긴다)
서버    BotInstance.session · judge_wallet(session) = "judge-wallet@<세션>"
```

실측 (2026-08-03):
```
세션 A 가 보는 봇   ['A의 봇']
세션 B 가 보는 봇   ['B의 봇']
세션 C 가 보는 봇   []
A → B의 봇 조회     404      A → B의 봇 삭제  404 (관리자 토큰이 있어도)
```

**③ 도 상당 부분 해결됐다** — 토큰을 알아도 남의 봇은 못 만진다.
남의 봇은 403 이 아니라 **404** 다. 403 은 "있긴 한데 권한이 없다"를
알려주는 셈이라, 남이 몇 개를 만들었는지 셀 수 있게 된다.

**경로 규칙은 손대지 않았다.** 역할(@ 앞)이 그대로 `judge-wallet` 이라
`core/routes.py` 의 화이트리스트가 세션 수와 무관하게 통한다 —
audit 9항목·verify 25항목 모두 그대로 통과한다.

**세션 없는 요청은 공용이다.** `verify_scenario.py`·`audit.py`·`/demo/seed`
는 헤더를 안 붙이고, 그렇게 만들어진 봇은 `session=""` 로 모두에게 보인다.
검증이 예전과 똑같이 동작해야 하기 때문이고, 운영 중에는 그런 봇이 생기지 않는다.

**한계**: 세션 ID 를 훔쳐 쓰면 그 사람 행세를 할 수 있다. 브라우저에
저장되는 값이라 그렇다. 실서비스라면 서버 세션과 로그인이 필요하고,
이건 심사 링크용 격리다.

### 21. 배포 준비 완료 — 서비스 하나로 합쳤다

**FastAPI 가 화면까지 서빙한다**(`app/main.py` 맨 아래 SPA 폴백).
정적 호스팅을 따로 두면 배포가 둘, 주소가 둘, CORS 설정이 하나 더 늘고
심사위원에게 줄 링크도 헷갈린다. 화면은 빌드된 파일 몇 개뿐이라 같이 준다.

```
https://<도메인>/          화면 (SPA · /bot/xxx 새로고침도 동작)
                /ui/*      앱 API
                /judge/*   지갑 시연
                /health    헬스체크 — 원장·추론·시세가 실제로 붙었는지까지
```

`web/.env.production` 의 `VITE_API_BASE=` (빈 값) 로 같은 출처를 쓴다.
개발은 그대로다 — vite dev 가 5173 에서 화면을, 서버가 8100 에서 API 를 맡고,
`web/dist` 가 없으면 서빙 코드가 아예 붙지 않는다.

**비밀 옮기기.** `.gitignore` 가 막는 것이 곧 기동에 필요한 것이다.

```powershell
py -3.13 deploy/pack_secrets.py     # → deploy/secrets.tar.gz (61KB, 지갑 192개 + .env)
```

이 스크립트가 하는 중요한 일: `devnet.json` 의 `fee_payer_path` 가 이 PC 의
절대경로(`C:\Users\...\.config\solana\id.json`)라 리눅스에는 없다. 그 키를
묶음 안으로 복사하고 경로를 상대경로로 바꾼다. **이걸 안 하면 서버가 기동하다 죽는다.**

**리허설 통과 (2026-08-03).** 빈 디렉터리에 `app/` · `web/dist/` 와 묶음만
풀고 띄웠더니:
```
{"ok":true,"ledger":"devnet","bots":0,"inference_live":true,"quotes_live":true}
/ 200 · /ui/overview 200 · /ui/meta 200
```

**반드시 지킬 것 넷** — 자세한 것은 `deploy/README.md`.
1. `--workers 1`. 봇·포지션이 프로세스 메모리에 있어서 워커가 둘이면
   요청마다 다른 세계를 본다.
2. `wallets/` 를 **영구 볼륨**에. 재배포 때 사라지면 devnet 자금은 남았는데
   그걸 움직일 키가 없어진다 = 자금 좌초. 가장 위험한 실수다.
3. **HTTPS**. 팬텀은 보안 컨텍스트에서만 주입된다. http 로 열면 지갑
   시연이 통째로 막힌다(localhost 는 예외).
4. devnet SOL 을 채워둘 것. 세션마다 지갑 하나, 봇마다 넷씩 늘어난다.

### 이 과정에서 고친 버그 넷

| 증상 | 원인 · 해결 |
|---|---|
| 봇 생성·조회가 전부 500 | `snapshot()` 이 전 지갑을 `getMultipleAccounts` **한 번**에 넣는데 RPC 상한이 100개다. 봇마다 지갑이 4개씩 늘어 25마리쯤에서 터진다. 100개씩 잘라 부르게 고침 (`devnet_ledger.py`) |
| 모델이 "허용 목록에 없는 종목" 이라며 확신도 0.0 | 프롬프트의 룰북에는 `NVDA`, 판단 요청에는 미러 토큰 `NVDAx` 를 보냈다. 모델 눈에는 다른 종목이다. `prompts.display_ticker()` 로 통일 |
| 실추론 판단이 항상 "판매 불가" | `receipt_complete` 조건이 `mode != "byok"` 이었다 — **모의 판단은 팔리고 진짜 Gemini 판단은 못 파는** 뒤집힌 규칙. x402 결제는 어느 모드에서든 똑같이 온체인에 남으므로, 기준을 `not degraded`(모의로 폴백했는가) 하나로 바꿈 |
| 지갑 시연에서 봇을 못 고름 (클릭 불가) | `JudgePanel` 이 봇 목록을 **한 번만** 조회했다(`useApi(..., [])`). 이 패널은 페이지가 열릴 때 함께 마운트되는데 그때는 봇이 0개다 — 봇을 만들고 돌아와도 목록이 영영 비어 있어 3·4단계가 눌리지 않는다. 4초 폴링으로 바꾸고, 고른 봇이 삭제되면 다시 고르게 함 |

---

## 지금 상태: 6화면 전부 실데이터로 동작

목업의 숫자는 하나도 남아 있지 않다. 화면의 모든 금액·수익률·비중·승률·
API 지출이 원장(`app/core/ledger.py`)과 저널(`app/core/journal.py`)에서
계산돼 나온다. 환율도 실시간이다(키 불필요).

### 다시 띄우는 법

터미널 두 개.

```powershell
# ① 백엔드 (리포지토리 루트)
$env:LEDGER_MODE="devnet"            # 온체인 시연이면 필수
$env:PYTHONIOENCODING="utf-8"        # 없으면 기동 로그에서 죽는다 (콘솔이 cp949)
py -3.13 -m uvicorn app.main:app --port 8100

# ② 프론트
cd web
npm run dev                          # → http://localhost:5173
```

Gemini 실추론은 `.env` 에서 켜진다(`INFERENCE_MODE=byok` + `GEMINI_API_KEY`).
`app/config.py` 가 기동할 때 읽고, **이미 설정된 환경변수는 덮지 않는다** —
셸에서 준 값이 파일보다 우선이다. 홈 화면 하단 배지와 옆 안내 패널이
지금 실추론인지 모의 판단인지 그대로 표시한다.

상태 파일(`state/app_state.mock.json`)이 남아 있어서 봇·포지션·거래기록이
그대로 복원된다. 처음부터 보고 싶으면:

```powershell
curl.exe -X POST http://127.0.0.1:8100/demo/seed -H "x-admin-token: dev-token"
curl.exe -X POST http://127.0.0.1:8100/bots/bot2/cycle -H "x-admin-token: dev-token"
```

---

## 무엇을 만들었나

### 백엔드에 추가 (자금 경로는 건드리지 않음)

| 파일 | 역할 |
|---|---|
| `app/ui.py` | 화면 전용 **조회** 라우터 `/ui/*` 13개 |
| `app/core/journal.py` | 체결·자산추이·API호출 **기록** |
| `app/core/profiles.py` | 봇 프로필 (표시용 설정, 집행되는 룰북과 분리) |
| `app/adapters/fx.py` | USD→KRW 환율 (open.er-api.com, 키 불필요) |

기존 파일 수정은 최소한이다 — `main.py`(기록 훅 3곳 + 라우터 등록),
`x402_client.py`(결제 1건 기록), `store.py`(저널·프로필 저장), `gemini_live.py`
(대화용 `converse()` 추가).

**왜 기록 계층이 필요했나**: 화면이 묻는 건 대부분 '지나온 것'인데
(거래 내역·자산 추이·API별 지출) 시스템은 '지금 상태'만 알고 있었다.
체결은 `BOOK.close`로 사라지고, 자산 추이는 아무도 안 남기고, API 지출은
합계만 있었다. 셋 다 사후에 복원할 수 없어서 일어나는 순간에 적는다.

### 프론트 (`web/`)

Vite + React + TS. 화면 6개 + 상세 탭 3개. 자세한 것은 `web/README.md`.

- 아이폰 목업 안에서 구동 (`DeviceFrame`) — 구멍이 디자인의 정확히 2배라 1:1로 들어간다
- 도입부: iOS 홈 화면 → SOL 스플래시 → 온보딩 (`IntroBoot`)
- 아이콘은 전부 Figma 원본. 손으로 그린 벡터 없음

---

## 검증된 것

```
verify_scenario.py   mock   23 통과 / 0 실패 / 2 미해당 (devnet 전용 항목)
verify_scenario.py   devnet 25 통과 / 0 실패 / 0 미해당
audit.py             devnet 9항목 전부 통과
web: typecheck·lint·build 전부 통과
```

전부 2026-08-02 저녁 '심사위원 단독 시연 모드' 작업 **뒤에** 다시 돌린 결과다.
영수증 판정(`receipts.effective_mode`)과 저널을 건드렸으므로 자금 경로가
그대로인지 확인이 필요했다.

앱이 부르는 순서 그대로 태운 통과 흐름 (실추론 · devnet):
봇 생성 → 지침 확인 → 사이클 1회(확신도 0.9 매수, 시도 1회) → 알림 1건 →
거래 내역 1건(서명 있음) → API 결제 5건(전부 서명, 추론 2건은 모델 ID 기록) →
대화 2건(투자 질문 답변 / 주식 외 질문 거절) → 봇 삭제까지.

devnet 결과 원본: `state/verify_result_devnet.json`.
devnet 실행에서만 확인되는 것 둘 — 분배 3건이 **하나의 온체인 시그니처**를 공유하고
(`A3b`), 확정 조회를 6회 강제 실패시켜도 이체는 1회만 집행된다(`D5`).

E2E로 확인한 흐름:
- 봇 생성 → 사이클 → 체결 → 청산 → 삭제
- 무인증 생성/삭제 403, 존재하지 않는 종목 400, 통화량 보존

**⚠️ 백엔드를 고쳤으면 반드시 `verify_scenario.py` 와 `audit.py` 를 다시 돌린다.**

---

## 심사위원 시연 패널 (2026-08-02 추가) — 온체인 실행까지 검증됨

아이폰 목업 **왼쪽**, 심사위원 패널 **오른쪽**. 심사위원 본인 지갑에서
devnet USDC 가 실제로 빠져나가는 것을 보여주는 화면.

### 왜 '위임(approve)' 인가

주소만 안다고 남의 지갑에서 돈을 뺄 수는 없다 — 개인키가 필요하다.
그래서 SPL 의 `approve` 를 쓴다. 심사위원이 **한 번** 서명해 한도를
위임하면, 그 뒤로는 봇이 자기 서명만으로 한도 안에서 인출한다.
자동이체 신청서와 같은 구조이고, 이게 곧 Track A 의 주장이다 —
사람의 승인은 위임 시점 한 번뿐이고 이후 결제는 에이전트 단독.

수수료는 우리 fee_payer 가 내므로 **심사위원은 devnet SOL 이 없어도 된다**
(`build_approve_tx` 가 부분 서명해 보내고 팬텀이 나머지를 채운다).

### 추가/수정된 것

| 파일 | 내용 |
|---|---|
| `app/judge.py` | 신규. `/judge/register·status·faucet·approve-tx·buy(SSE)` |
| `app/adapters/devnet_ledger.py` | `external_owners` · `register_external` · `build_approve_tx` · `delegate_status` · `delegated_transfer` · `fund_external` |
| `app/core/routes.py` | `judge-wallet` 역할 2경로 추가 |
| `app/main.py` | judge 라우터 등록 |
| `web/src/components/JudgePanel.tsx` | 신규. 4단계 + SSE 로그 |
| `web/src/api/judge.ts` · `web/src/lib/phantom.ts` | 신규 |
| `web/src/components/DeviceFrame.tsx` | `aside` prop — row/stack/bare 3배치 |

주의한 것 둘:
- **테스트 코인을 발행하지 않는다.** `external` 지갑에서 이체한다.
  발행하면 통화량이 늘어 `audit_supply` 의 보존 검증이 깨진다.
- **`snapshot()` 이 외부 지갑도 센다.** 안 그러면 심사위원→user-treasury
  예치가 '없던 돈이 생긴 것' 으로 보여 같은 검증이 깨진다.

### devnet 재구축 완료 (2026-08-02)

기존 `wallets/devnet.json` 의 `fee_payer_path` 가 다른 PC 경로
(`C:\Users\shyh1\...`)라 이 머신에서는 devnet 모드가 기동조차 안 됐다.
그 키가 수수료 지불자이자 민트 권한자였다. 복구가 불가능해 **새로 만들었다.**

```
수수료·민트 권한  C:\Users\sohgs\.config\solana\id.json
                 Hf6FjLQKxn6rBwJsVP1re8KrgppPyxfYAqrii8Uy1Ewj
새 USDC 민트      FzixWBtZoVFWdfLN5QaG8YqQnaWR4zDpywJGakt4PQSW
옛 설정 백업      wallets/devnet.json.pre-20260802-new
```

공용 RPC 에어드랍은 IP 단위로 막힌다 — SOL 은 https://faucet.solana.com
에서 수동으로 받아야 한다. 부트스트랩은 14/14 지갑 충전, 미러토큰 4종
각 1,000 개 발행으로 완료됐다.

기동:
```powershell
$env:LEDGER_MODE="devnet"; $env:PYTHONIOENCODING="utf-8"
py -3.13 -m uvicorn app.main:app --port 8100
```

### 검증 상태

**전 흐름이 온체인에서 동작한다.** 브라우저에서 팬텀 서명까지 포함해
① 연결 → ② 지급 → ③ 위임 서명 1회 → ④ 매수(서명 없음) 를 완주했다.

장부와 온체인을 소수점 6자리까지 대조한 결과 (2026-08-02):
```
bot1  NVDAx  장부 0.346561  온체인 0.346561  ✅ 3bM7sAP9…
bot2  AAPLx  장부 0.094705  온체인 0.094705  ✅ 38jhpkog…
bot3  MSFTx  장부 0.116883  온체인 0.116883  ✅ 22vp8fpZ…
```
API 결제도 호출 1건당 온체인 트랜잭션 1건으로 남는다
(`research-agent@*` 토큰계정의 서명 이력으로 확인).

강제 매수는 mock 원장에서 편향 없음 0/6 vs 편향 있음 **6/6 체결**.

### 밟았던 함정 (같은 것을 또 밟지 않도록)

| 증상 | 원인 · 해결 |
|---|---|
| 팬텀 `Unexpected error` | ①`/judge/register` 가 형식만 봐서 **민트 주소**가 지갑으로 등록됐다 → owner 자리에 민트가 박혀 팬텀이 서명할 자리를 못 찾음. `assert_wallet()` 로 시스템 프로그램 소유 여부를 확인하게 고침. ②`@solana/web3.js` 가 쓰는 `Buffer` 가 브라우저에 없어 `deserialize()` 가 터짐 → `web/src/polyfills.ts` |
| 장부에 있는데 온체인엔 없는 포지션 | mock 시절 포지션(`tx` 가 `pay_…`)이 devnet 상태 파일에 섞임. 상태 파일은 **모드별로 분리**된다 (`state/app_state.{mock,devnet}.json`) — 진단할 때 파일을 잘못 보면 한참 헤맨다. 정리본 백업: `app_state.devnet.json.pre-clean` |
| 시연 반복하면 매수가 안 됨 | 일일 거래·지출 카운터와 **총 노출 한도**($300). 앞의 둘은 `_preflight` 가 리셋하고, 노출은 리셋으로 못 풀어 실제 청산이 필요하다 (`close-open` 단계) |

---

## 다음에 할 일 (우선순위 순)

### 1. 남은 에셋 받기 — 화면에 빈 자리가 있다
- **Exa · ExchangeRate · 신한은행 · Toss 로고**: 남의 상표라 지어낼 수 없어
  이름 첫 글자 모노그램으로 대체해 뒀다. 파일이 생기면
  `web/src/components/ProviderMark.tsx` 한 곳만 고치면 된다.

### 2. Gemini 키 할당량 — 시연 중에 걸릴 수 있다
지금 키는 free tier 다. Pro 계열은 아예 막혀 있고(`limit: 0`),
`gemini-3.6-flash` 도 연달아 부르면 429가 난다. 폴백은 안전하지만 그때
화면 배지가 `모의 판단` 으로 바뀌고 그 사이클의 판단은 팔리지 않는다
(영수증이 뒷받침 못 하므로). 발표 전에 결제 계정으로 올리거나,
시연 직전 리허설을 몰아서 돌리지 않는 편이 낫다.

### 2-b. (완료) 시장 확대 — 4개 시장 76종

코스피 20 · 코스닥 20 · 나스닥 20 · 닛케이 20 종목을 미러 토큰으로 발행하는
작업은 **시세 없이 하면 안 된다.** 발행만 하고 가격이 없으면 `spot()` 이
기본값 $100 을 돌려주는데, 그건 이 프로젝트가 하지 않기로 한 지어낸 숫자다.
지금 4종의 기준가는 실측값이라 괜찮지만 새 60종에는 그런 근거가 없다.

받은 키는 **한국투자증권 KIS OpenAPI 의 appkey** 로 확인됐다
(토큰 엔드포인트가 appkey 는 통과시키고 `EGW00105 유효하지 않은
AppSecret입니다` 만 반환). 어댑터는 `app/adapters/kis_quotes.py` 에 다 있고,
`.env` 의 `KIS_APP_SECRET` 만 채우면 켜진다.

secret 이 오면 순서는 이렇다.
1. `KIS_APP_SECRET` 설정 → 토큰 발급 확인 (국내 1종·해외 1종 조회)
2. 지수별 상위 20종목 확정 → `app/core/markets.py` 의 시장에 채운다
3. `bootstrap_devnet.py` 로 미러 민트 발행 (60~80종 ≈ 0.3 SOL)
   - ⚠️ `ensure_bot_wallets` 가 지금은 봇마다 **전 종목** ATA 를 미리 만든다.
     4종일 때는 괜찮았지만 80종이면 봇 하나 만드는 데 계정 80개다.
     `swap_in` 이 매수 시점에 멱등 생성하도록 먼저 바꿔야 한다.
4. `external.spot()` 을 KIS 우선으로 — 실패 시 내장 기준가 폴백
5. 시장의 `tradable` 을 '시세를 받아올 수 있는가'로 판정

수수료 지갑 잔고는 현재 **0.74 SOL** 이다. 60종 발행에는 충분하지만
넉넉하지 않다 — https://faucet.solana.com 에서 미리 더 받아두는 편이 낫다.

### 3. 출금 경로 — 제품상 구멍
봇을 지워도 잔액이 그 봇 지갑에 남는다. `core/routes.py` 허용 경로에
`user-treasury → external` 이 없고 `audit.py` 6번이 그걸 검증하기 때문이다.
붙이려면 그 규칙과 감사 항목을 함께 손봐야 한다. 지금은 삭제 확인 시트가
이 사실을 그대로 띄운다.

### 4. 자산 추이 데이터 쌓기
곡선은 서버가 실제 관측한 점만 그린다. '3개월' 을 제대로 보여주려면 그만큼
돌려야 한다. 발표 전에 스케줄러를 미리 켜두면 점이 쌓인다:
```powershell
curl.exe -X POST "http://127.0.0.1:8100/scheduler/start?interval_seconds=60" -H "x-admin-token: dev-token"
```

### 5. 하단 탭·헤더 아이콘 그룹화 (선택)
Figma에서 `tap`·`onboarding icon right` 그룹은 받았지만 **아이콘 하나당
그룹**은 아직 없다. 지금은 그룹 전체를 내보낸 뒤 viewBox로 잘라 쓰는데,
아이콘마다 그룹이 생기면 그 단계 없이 바로 받을 수 있다.

### 6. 배포
아직 로컬만 돌려봤다. 심사용 링크로 내보내려면 백엔드 호스팅 + 프론트
`VITE_API_BASE` 설정이 필요하다.

---

## 밟으면 아픈 곳 (다시 안 밟도록)

| 증상 | 원인 · 해결 |
|---|---|
| 서버가 기동 로그에서 죽음 | 콘솔이 cp949. `PYTHONIOENCODING=utf-8` |
| `npm run dev` 가 vite 를 못 찾음 | 경로의 `&` 때문에 cmd 가 PATH 를 자름. 스크립트가 `node` 로 직접 부르게 해둠 |
| 아이콘이 파란 사각형으로 나옴 | Figma export 에 딸려 온 배경 rect. `#F5F5F5`·`#EFF4FB`·`#FEFEFF`·`#DFE7EF` 를 걷어낼 것 |
| 특정 라우트에서만 아이콘이 사라짐 | 4KB 넘는 SVG가 파일로 빠지며 상대경로 404. `assetsInlineLimit: 40KB` 로 해결됨 |
| 아이콘 모양·굵기가 원본과 다름 | 조각을 퍼센트로 재조립하지 말 것. 노드를 통째로 export |
| `--mask-src` 가 안 먹음 | React 19 가 인라인 style 의 `url(...)` 을 버린다. CSS 클래스에 둘 것 |

---

## 커밋 상태

작업 내용은 **아직 커밋하지 않았다.** 작업 트리에 그대로 있다.

```
 M README.md  app/main.py  app/external.py  app/config.py
   app/core/{store,x402_client,journal,profiles,receipts}.py
   app/core/routes.py  app/agents/pipeline.py
   app/adapters/{gemini_live,devnet_ledger}.py
 ?? app/ui.py  app/judge.py  app/core/{prompts,events}.py
 ?? app/adapters/{fx,gemini_byok}.py
 ?? web/   (node_modules·dist 는 .gitignore 처리됨)
```

⚠️ `.env` 에 Gemini API 키가 있다. `.gitignore` 에 넣어뒀지만, 커밋 전에
`git status` 로 한 번 더 확인할 것.
