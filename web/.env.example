# 복사해서 .env 로 쓰면 된다. 둘 다 없어도 기본값으로 동작한다.

# API 주소. 기본값은 /api 이고 vite.config.ts 가 127.0.0.1:8100 으로 프록시한다.
# 백엔드를 다른 곳에 띄웠다면 여기에 전체 주소를 적는다.
VITE_API_BASE=/api

# 봇 생성·수정·정지에 필요한 관리자 토큰. 백엔드 app/config.py 의 ADMIN_TOKEN 과 같아야 한다.
# ⚠️ 브라우저 번들에 그대로 들어간다 — 데모 전용이다.
VITE_ADMIN_TOKEN=dev-token
